# Enable the GM8775C MIPI-DSI -> LVDS bridge driver (added out-of-tree via the
# devtool workflow: drivers/gpu/drm/bridge/gm8775c.c + Kconfig/Makefile).
# Without this the DRM_GM8775C symbol stays 'n' and the .o is never built.
CONFIG_DRM_GM8775C=y
