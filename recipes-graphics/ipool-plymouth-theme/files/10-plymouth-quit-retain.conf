# Make the stock plymouth-quit.service retain the splash frame instead of
# blanking the screen, so if it fires before the Qt app draws we don't get a
# black flash. The real DRM hand-off is driven by b2qt.service's ExecStartPre.
[Service]
ExecStart=
ExecStart=-/usr/bin/plymouth quit --retain-splash
