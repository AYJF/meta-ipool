[Service]
# Fill in a per-board target_name before the updater starts. '+' forces root
# so it can edit /etc/rauc-hawkbit-updater/config.conf regardless of User=.
ExecStartPre=+/usr/bin/hawkbit-set-target-id
